<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link rel="stylesheet" href="style.css"> <!-- Link to your CSS -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        header {
            background: #35424a;
            color: #ffffff;
            padding: 20px;
            text-align: center;
        }

        h3 {
            margin: 0;
        }

        nav {
            margin: 20px 0;
        }

        nav a {
            color: #ffffff;
            text-decoration: none;
            margin: 0 15px;
            padding: 10px 15px;
            border-radius: 5px;
            background: #5bc0de;
            transition: background 0.3s;
        }

        nav a:hover {
            background: #31b0d5;
        }

        h2 {
            color: #35424a;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            background: #ffffff;
            border-radius: 5px;
            overflow: hidden;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #dddddd;
        }

        th {
            background-color: #35424a;
            color: #ffffff;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        .booking-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #5cb85c;
            color: white;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            transition: background 0.3s;
        }

        .booking-button:hover {
            background-color: #4cae4c;
        }
    </style>
</head>
<body>

    <header>
        <h3>Welcome, <%= user.first_name %>!</h3>
        <nav>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <h2>Your Bookings</h2>
    <table>
        <tr>
            <th>Doctor</th>
            <th>Date</th>
            <th>Time</th>
            <th>Notes</th>
        </tr>
        <% bookings.forEach(booking => { %>
            <tr>
                <td><%= booking.doctor %></td>
                <td><%= booking.date %></td>
                <td><%= booking.time %></td>
                <td><%= booking.notes %></td>
            </tr>
        <% }) %>
    </table>

    <h2>Book a New Session</h2>
    <a class="booking-button" href="/booking">Book a Session</a> <!-- Link to the booking page -->

</body>
</html>
